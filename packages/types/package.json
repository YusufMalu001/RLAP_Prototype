{
  "name": "@rlap/types",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript types for RLAP (cart, booking, screens, enums)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
